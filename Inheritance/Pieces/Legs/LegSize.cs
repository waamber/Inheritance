namespace Inheritance.Pieces.Legs
{
    enum LegSize //NOT A CLASS -- you can define options
    {
        Baby = 1,  //0
        Child, //1
        Adult  //2
        //set Baby to 1 bc O is the default. if it's 0, LegSize will always start as LegSize.Baby (hover over them, Amber!)
    }
}
